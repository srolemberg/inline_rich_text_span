name: inline_rich_text_span
description: "Provides a Widget wrapper for RichText to stylize text and add behavior."
version: 1.0.2
homepage: https://github.com/srolemberg/inline_rich_text_span

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
